// Existing helper — predates the @zeal-checked convention, so it is not
// retroactively tagged (see .dsh/skills/zeal-checked/SKILL.md / AGENTS.md:
// the tag applies to newly added functions only).
export function shout(input: string): string {
  return input.toUpperCase() + '!'
}

// titleCase(input: string): string goes here — see task.txt.
