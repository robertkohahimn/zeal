# Zeal Gauntlet

Recorded results of running Zeal's offline task gauntlet
(`gauntlet/run.sh <task-dir>`) — an unattended, machine-driven harness that
packs the `@zealagent/dsh-zeal` bundle, installs it into a fresh scratch
`zeal` profile with the `gauntlet/overlay.cordis.yml` overlay applied (TUI
disabled, `zeal-gauntlet-runner` driving the task instead), copies the
task's `repo/` fixture into a scratch workdir, runs the task through a real
model, and then runs the task's own `verify.sh` to decide pass/fail.

Each row below is one recorded run. `run.sh`'s own exit code (and thus the
row's Outcome) is `verify.sh`'s exit code, not the model's self-reported
turn-completion status.

Some tasks (G5, G7) extend the shared overlay via two optional per-task
hooks `run.sh` looks for: `overlay.extra.cordis.yml` (appended verbatim
onto the profile's `cordis.patch.yml` after the shared overlay) and an
executable `setup.sh` (run after the task's `repo/` is copied into
`$WORKDIR`, for hooks that need a runtime-computed absolute path or need to
install an extra plugin) — see `gauntlet/run.sh`'s own step-4b comment for
the full mechanism. G8 has no `repo/`/`verify.sh` at all; it is a scripted
manual procedure (`gauntlet/tasks/g8-interrupt/manual.md`) for the
interactive TUI, which the unattended `zeal-gauntlet-runner` driver never
mounts.

| Task | Date | Model | Bundle version | dsh version | Outcome | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| G1 (`gauntlet/tasks/g1-fix-test`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. Everything up to the model call (the `zeal-gauntlet-runner` plugin + its unit tests, the `gauntlet/overlay.cordis.yml` overlay, `gauntlet/run.sh`, the G1 fixture repo + `verify.sh`, and an overlay-composition `--dump-config` check reusing Task 16's helpers) is built and verified. To record this row for real, run:<br>`ZAI_API_KEY=<key> gauntlet/run.sh gauntlet/tasks/g1-fix-test` |
| G2 (`gauntlet/tasks/g2-refactor`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. `getUser`/`getUserSync` duplicated across `src/serviceAlpha.ts`/`serviceBeta.ts`/`serviceGamma.ts`, with `src/callers.ts` inconsistently mixing sync/async callers — task asks to unify into one canonical async `src/userApi.ts` and rewire every caller. `verify.sh` (`cd $WORKDIR && npx vitest run`) mechanically confirmed: FAILs (`Cannot find module '../src/userApi.ts'`) against the unmodified fixture, PASSes (5/5) against a hand-fixed copy. To record this row for real, run:<br>`ZAI_API_KEY=<key> gauntlet/run.sh gauntlet/tasks/g2-refactor` |
| G3 (`gauntlet/tasks/g3-plan-todo`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. Task text starts "Plan first, then implement:" and asks for a `--json` flag on the repo's small CLI. `verify.sh` runs vitest AND `grep -rq 'todo_write' "$DSH_HOME/sessions"` (recursive — session-persistence-jsonl nests two levels deep under root; `gauntlet/run.sh` now exports `DSH_HOME` to `verify.sh`, not just `WORKDIR`). Mechanically confirmed: vitest FAILs against the unmodified CLI; with a hand-fixed CLI, the grep step FAILs against an empty scratch session store and PASSes once a fake `todo_write` tool/call session event is present. To record this row for real, run:<br>`ZAI_API_KEY=<key> gauntlet/run.sh gauntlet/tasks/g3-plan-todo` |
| G4 (`gauntlet/tasks/g4-subagent`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. Task asks the model to delegate a `src/` TODO-comment audit to a subagent, then fix the three TODO-marked bugs (off-by-one range count, inverted discount sign, no-op `unique()`) it reports. `verify.sh` runs vitest AND `grep -rq '"subagent"' "$DSH_HOME/sessions"` (tool-subagent's default model-facing tool name is `subagent`). Mechanically confirmed: vitest FAILs (3/3) against the unmodified fixture; with hand-fixed code, the grep step FAILs against an empty session store and PASSes once a fake `"subagent"` tool/call event is present. To record this row for real, run:<br>`ZAI_API_KEY=<key> gauntlet/run.sh gauntlet/tasks/g4-subagent` |
| G5 (`gauntlet/tasks/g5-mcp`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. This IS the spec §4.5 MCP doc-flow rehearsal — `repo/mcp-server.mjs` is a stdio MCP server (`@modelcontextprotocol/sdk`) exposing `lookup_constant` (`{"ZEAL_MAGIC": 271828}`); the task's own `setup.sh` (new `gauntlet/run.sh` per-task hook) runs `dsh plugin --profile zeal add @deepseek-ai/dsh-mcp-client` then appends an `insert:` row wiring it to `mcp-server.mjs` by absolute path; task asks the model to call the tool and write its value into `constant.txt`; `verify.sh` greps for `271828`. Mechanically verified (no model): a standalone `@modelcontextprotocol/sdk` client performed a REAL stdio `initialize` + `tools/list` + `tools/call` handshake against `mcp-server.mjs` — `initialize: OK`, `tools/list: OK — ["lookup_constant"]`, `tools/call lookup_constant: OK — {"content":[{"type":"text","text":"{\"ZEAL_MAGIC\":271828}"}]}`, `PASS: lookup_constant returned ZEAL_MAGIC=271828`; `setup.sh`'s node_modules-symlink and row-append steps independently proven (no `ERR_MODULE_NOT_FOUND`; appended row parses to the expected `{transport: stdio, command: node, args: [<path>]}` shape); `verify.sh` FAILs on the unmodified fixture and PASSes against a hand-written `constant.txt`. To record this row for real, run:<br>`ZAI_API_KEY=<key> gauntlet/run.sh gauntlet/tasks/g5-mcp` |
| G6 (`gauntlet/tasks/g6-skill`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. `repo/.dsh/skills/zeal-checked/SKILL.md` (real discovery path per `$DSH_SRC/packages/skill/skill-filesystem/README.md`'s rank-100 `project-dsh` root) mandates a `@zeal-checked` JSDoc tag on every newly added function; `repo/AGENTS.md` restates it as a fallback since it is unconfirmed whether `dsh-skill-filesystem`/`dsh-tool-skill` are composed into the zeal bundle. Task asks for a new `titleCase()` function; `verify.sh` runs vitest AND greps `$WORKDIR/src` for the tag. Mechanically confirmed: vitest FAILs (2/2) against the unmodified fixture; with a hand-fixed, untagged `titleCase`, vitest PASSes but the grep step FAILs; both PASS once the tag is added. To record this row for real, run:<br>`ZAI_API_KEY=<key> gauntlet/run.sh gauntlet/tasks/g6-skill` |
| G7 (`gauntlet/tasks/g7-compaction`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. `repo/src` has eight ~190-line pipeline-stage files plus one deliberately-failing test (an inverted comparison in `stageC.ts`'s `applyMemberDiscount`); task asks the model to summarize all eight files then fix the one failing test. `overlay.extra.cordis.yml` (new `gauntlet/run.sh` per-task hook) restates the FULL `llm-pi-ai` row (both providers' `apiKeyEnv`/`reasoning`, patches replace the whole row) plus a per-route model reshape (spec A11) giving `zai`'s `glm-5.2` a `contextWindow: 16384` so compaction triggers cheaply. `verify.sh` runs vitest AND greps for a `compaction/end` session event (exact type name transcribed from `$DSH_SRC/packages/compaction/compaction-basic/src/region.ts`'s `session.append('compaction/end', ...)` calls). Mechanically confirmed: vitest FAILs with exactly 1/27 tests failing against the unmodified fixture; with the fix, vitest PASSes (27/27) but the grep step FAILs against an empty session store and PASSes once a fake `compaction/start`+`compaction/end` session event pair is present; `overlay.extra.cordis.yml` independently confirmed to append onto `gauntlet/overlay.cordis.yml` and both parse correctly (exact expected `providers`/`models` shape, shared overlay rows intact). To record this row for real, run:<br>`ZAI_API_KEY=<key> gauntlet/run.sh gauntlet/tasks/g7-compaction` |
| G8 (`gauntlet/tasks/g8-interrupt`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY + interactive terminal)** | Not yet run: no `ZAI_API_KEY` AND no interactive terminal are available in this environment (this task exercises the interactive TUI's Esc-to-interrupt flow, which `zeal-gauntlet-runner` deliberately never mounts). Recorded as a scripted manual procedure instead of `repo/`+`verify.sh`: `gauntlet/tasks/g8-interrupt/manual.md` — start zeal interactively in a scratch repo, ask for a long task, press Esc mid-turn, confirm the status bar (`packages/dsh-zeal/src/tui/ui/StatusBar.tsx`'s `⏵ running (esc interrupts)` / `idle` hints) returns to idle promptly, type a redirect message, confirm the next turn acts on the redirect rather than resuming the original task — cited against `App.tsx`'s Esc handler, `driver.ts`'s `interrupt()` (`liveAgent.cancel({kind:'user'},{keepInbox:true})`), and `dsh-agent`'s `CancelOptions`/`AgentStatus` doc. To record this row for real, follow `gauntlet/tasks/g8-interrupt/manual.md`'s numbered procedure with a real `ZAI_API_KEY` and an interactive terminal, and check off its pass/fail checklist. |
| smoke (`packages/dsh-zeal/tests/composition/live-smoke.test.ts`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. The live-call test is double-gated (`ZAI_API_KEY` AND `ZEAL_COMPOSITION`) and confirmed to skip cleanly without a key — verified for real in this environment, along with a separate fake-credential probe (`ZEAL_COMPOSITION` only, no real key) that installs the same composed profile+overlay and confirms the run fails at the provider's own auth check (`dsh: AUTH: 401: {"code":"401","message":"token expired or incorrect"}`), proving the full chain up to the API boundary. To record this row for real, run:<br>`ZAI_API_KEY=<key> ZEAL_COMPOSITION=1 pnpm vitest run packages/dsh-zeal/tests/composition/live-smoke.test.ts` |
