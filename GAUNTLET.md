# Zeal Gauntlet

Recorded results of running Zeal's offline task gauntlet
(`gauntlet/run.sh <task-dir>`) — an unattended, machine-driven harness that
packs the `@zealagent/dsh-zeal` bundle, installs it into a fresh scratch
`zeal` profile with the `gauntlet/overlay.cordis.yml` overlay applied (TUI
disabled, `zeal-gauntlet-runner` driving the task instead), copies the
task's `repo/` fixture into a scratch workdir, runs the task through a real
model, and then runs the task's own `verify.sh` to decide pass/fail.

Each row below is one recorded run. `run.sh`'s own exit code (and thus the
row's Outcome) is `verify.sh`'s exit code, not the model's self-reported
turn-completion status.

Some tasks (G5, G7) extend the shared overlay via two optional per-task
hooks `run.sh` looks for: `overlay.extra.cordis.yml` (appended verbatim
onto the profile's `cordis.patch.yml` after the shared overlay) and an
executable `setup.sh` (run after the task's `repo/` is copied into
`$WORKDIR`, for hooks that need a runtime-computed absolute path or need to
install an extra plugin) — see `gauntlet/run.sh`'s own step-4b comment for
the full mechanism. G8 has no `repo/`/`verify.sh` at all; it is a scripted
manual procedure (`gauntlet/tasks/g8-interrupt/manual.md`) for the
interactive TUI, which the unattended `zeal-gauntlet-runner` driver never
mounts.

| Task | Date | Model | Bundle version | dsh version | Outcome | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| G1 (`gauntlet/tasks/g1-fix-test`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. Everything up to the model call (the `zeal-gauntlet-runner` plugin + its unit tests, the `gauntlet/overlay.cordis.yml` overlay, `gauntlet/run.sh`, the G1 fixture repo + `verify.sh`, and an overlay-composition `--dump-config` check reusing Task 16's helpers) is built and verified. To record this row for real — with `ZAI_API_KEY` exported in your environment, never inline on the command (shell history and CI command logs capture command text) — run:<br>`gauntlet/run.sh gauntlet/tasks/g1-fix-test` |
| G2 (`gauntlet/tasks/g2-refactor`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. `getUser`/`getUserSync` duplicated across `src/serviceAlpha.ts`/`serviceBeta.ts`/`serviceGamma.ts`, with `src/callers.ts` inconsistently mixing sync/async callers — task asks to unify into one canonical async `src/userApi.ts` and rewire every caller. `verify.sh` (`cd $WORKDIR && npx vitest run`) mechanically confirmed: FAILs (`Cannot find module '../src/userApi.ts'`) against the unmodified fixture, PASSes (5/5) against a hand-fixed copy. **Strengthened (review fix):** `test/api.test.ts`'s assertions alone could be satisfied WITHOUT performing the migration — the three duplicated services already return the same users, so merely marking `greetAlpha`/`greetGamma` `async` while still importing `getUserSync` from `serviceAlpha`/`serviceGamma` passed every check, and `src/userApi.ts` only had to exist. `test/migration.test.ts` closes that hole from both ends: a sentinel `vi.mock` of `../src/userApi.ts` that only a caller genuinely routed through the unified module can return, plus source-level checks that `src/callers.ts` imports none of the three old service modules and that no service file still declares `getUser`/`getUserSync` (deleting the file counts as removal). The structural matchers were confirmed to flag the sham fix and accept the real one; the sentinel-mock half is standard `vi.mock` hoisting and has not yet been executed (this environment has no Node >=22 to run vitest 3). To record this row for real — with `ZAI_API_KEY` exported in your environment, never inline on the command (shell history and CI command logs capture command text) — run:<br>`gauntlet/run.sh gauntlet/tasks/g2-refactor` |
| G3 (`gauntlet/tasks/g3-plan-todo`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. Task text starts "Plan first, then implement:" and asks for a `--json` flag on the repo's small CLI. `verify.sh` runs vitest AND a STRUCTURAL session-event check via `gauntlet/lib/find-session-event.mjs` (`gauntlet/run.sh` exports `DSH_HOME` to `verify.sh`, not just `WORKDIR`). **Corrected (review fix):** it used to be `grep -rq 'todo_write' "$DSH_HOME/sessions"`, but `session.jsonl` records user and assistant messages verbatim — so the task prompt itself, or a model that merely narrated "I'll use todo_write" without calling it, satisfied the grep. The helper now parses each JSONL record and matches on the event's own `type`/`data` fields: a `tool/call` with `data.name == "todo_write"`, or the dedicated `todo/write` event (either is legitimate proof). Re-confirmed mechanically: the real `verify.sh` FAILs against a prose-only session that mentions the tool and PASSes against one carrying the actual event. Mechanically confirmed: vitest FAILs against the unmodified CLI; with a hand-fixed CLI, the grep step FAILs against an empty scratch session store and PASSes once a fake `todo_write` tool/call session event is present. To record this row for real — with `ZAI_API_KEY` exported in your environment, never inline on the command (shell history and CI command logs capture command text) — run:<br>`gauntlet/run.sh gauntlet/tasks/g3-plan-todo` |
| G4 (`gauntlet/tasks/g4-subagent`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. Task asks the model to delegate a `src/` TODO-comment audit to a subagent, then fix the three TODO-marked bugs (off-by-one range count, inverted discount sign, no-op `unique()`) it reports. `verify.sh` runs vitest AND a STRUCTURAL session-event check via `gauntlet/lib/find-session-event.mjs`, requiring a `tool/call` whose `data.name` is `subagent` (tool-subagent's default model-facing tool name). **Corrected (review fix):** it used to be `grep -rq '"subagent"' "$DSH_HOME/sessions"`, a substring test the recorded assistant/user message text could satisfy on its own. Re-confirmed mechanically: FAILs against a prose-only session, PASSes against one carrying the real `tool/call` event. Mechanically confirmed: vitest FAILs (3/3) against the unmodified fixture; with hand-fixed code, the grep step FAILs against an empty session store and PASSes once a fake `"subagent"` tool/call event is present. To record this row for real — with `ZAI_API_KEY` exported in your environment, never inline on the command (shell history and CI command logs capture command text) — run:<br>`gauntlet/run.sh gauntlet/tasks/g4-subagent` |
| G5 (`gauntlet/tasks/g5-mcp`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. This IS the spec §4.5 MCP doc-flow rehearsal — `repo/mcp-server.mjs` is a stdio MCP server (`@modelcontextprotocol/sdk`) exposing `lookup_constant` (`{"ZEAL_MAGIC": 271828}`); the task's own `setup.sh` (new `gauntlet/run.sh` per-task hook) runs `dsh plugin --profile zeal add @deepseek-ai/dsh-mcp-client` then appends an `insert:` row wiring it to `mcp-server.mjs` by absolute path; task asks the model to call the tool and write its value into `constant.txt`; `verify.sh` requires the file's whitespace-trimmed contents to equal `271828` EXACTLY. **Corrected (review fix):** it used to be `grep -q 271828`, which also accepted content merely embedding the digits (`x271828`, `1271828`, a stray log line); `task.txt` now states the exact-contents contract so the requirement is unambiguous from the model's side too. Mechanically verified (no model): a standalone `@modelcontextprotocol/sdk` client performed a REAL stdio `initialize` + `tools/list` + `tools/call` handshake against `mcp-server.mjs` — `initialize: OK`, `tools/list: OK — ["lookup_constant"]`, `tools/call lookup_constant: OK — {"content":[{"type":"text","text":"{\"ZEAL_MAGIC\":271828}"}]}`, `PASS: lookup_constant returned ZEAL_MAGIC=271828`; `setup.sh`'s node_modules-symlink and row-append steps independently proven (no `ERR_MODULE_NOT_FOUND`; appended row parses to the expected `{transport: stdio, command: node, args: [<path>]}` shape); `verify.sh` FAILs on the unmodified fixture and PASSes against a hand-written `constant.txt`. To record this row for real — with `ZAI_API_KEY` exported in your environment, never inline on the command (shell history and CI command logs capture command text) — run:<br>`gauntlet/run.sh gauntlet/tasks/g5-mcp` |
| G6 (`gauntlet/tasks/g6-skill`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. `repo/.dsh/skills/zeal-checked/SKILL.md` (real discovery path per `$DSH_SRC/packages/skill/skill-filesystem/README.md`'s rank-100 `project-dsh` root) mandates a `@zeal-checked` JSDoc tag on every newly added function; `repo/AGENTS.md` restates it as a fallback since it is unconfirmed whether `dsh-skill-filesystem`/`dsh-tool-skill` are composed into the zeal bundle. Task asks for a new `titleCase()` function; `verify.sh` runs vitest AND a STRUCTURAL tag check. **Corrected (review fix):** the tag step used to be `grep -rq '@zeal-checked' "$WORKDIR/src"`, which could never fail — `repo/src/format.ts`'s own header comment contains the literal string `@zeal-checked` (the note explaining why the pre-existing `shout` is deliberately untagged), so the recursive grep matched the untouched fixture. The earlier "with a hand-fixed, untagged `titleCase` … the grep step FAILs" entry in this row was therefore wrong. It now parses each non-test file under `src/` and requires the tag inside the JSDoc block IMMEDIATELY above each newly added function (pre-existing `shout` stays exempt, per the convention's "newly written functions only"). Re-confirmed mechanically against the real `verify.sh`: FAILs on the unmodified fixture (no `titleCase` declared), FAILs with an untagged `titleCase`, FAILs when the tag is separated from the declaration by a blank line, FAILs when a second new function is left untagged, and PASSes for both the `SKILL.md` example form and an equivalent tagged arrow function in a new file. To record this row for real — with `ZAI_API_KEY` exported in your environment, never inline on the command (shell history and CI command logs capture command text) — run:<br>`gauntlet/run.sh gauntlet/tasks/g6-skill` |
| G7 (`gauntlet/tasks/g7-compaction`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. `repo/src` has eight ~190-line pipeline-stage files plus one deliberately-failing test (an inverted comparison in `stageC.ts`'s `applyMemberDiscount`); task asks the model to summarize all eight files then fix the one failing test. `overlay.extra.cordis.yml` (new `gauntlet/run.sh` per-task hook) restates the FULL `llm-pi-ai` row (both providers' `apiKeyEnv`/`reasoning`, patches replace the whole row) plus a per-route model reshape (spec A11) giving `zai`'s `glm-5.2` a `contextWindow: 16384` so compaction triggers cheaply. `verify.sh` runs vitest AND a STRUCTURAL session-event check via `gauntlet/lib/find-session-event.mjs`, requiring a record whose `type` is `compaction/end` (exact type name transcribed from `$DSH_SRC/packages/compaction/compaction-basic/src/region.ts`'s `session.append('compaction/end', ...)` calls). **Corrected (review fix):** it used to be `grep -rq '"compaction/end"' "$DSH_HOME/sessions"` — narrower than G3's unquoted needle, but still a substring test that recorded assistant text could satisfy. Mechanically confirmed: vitest FAILs with exactly 1/27 tests failing against the unmodified fixture; with the fix, vitest PASSes (27/27) while the event check FAILs against an empty session store and against a prose-only session, and PASSes once a real `compaction/end` record is present; `overlay.extra.cordis.yml` independently confirmed to append onto `gauntlet/overlay.cordis.yml` and both parse correctly (exact expected `providers`/`models` shape, shared overlay rows intact). To record this row for real — with `ZAI_API_KEY` exported in your environment, never inline on the command (shell history and CI command logs capture command text) — run:<br>`gauntlet/run.sh gauntlet/tasks/g7-compaction` |
| G8 (`gauntlet/tasks/g8-interrupt`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY + interactive terminal)** | Not yet run: no `ZAI_API_KEY` AND no interactive terminal are available in this environment (this task exercises the interactive TUI's Esc-to-interrupt flow, which `zeal-gauntlet-runner` deliberately never mounts). Recorded as a scripted manual procedure instead of `repo/`+`verify.sh`: `gauntlet/tasks/g8-interrupt/manual.md` — start zeal interactively in a scratch repo, ask for a long task, press Esc mid-turn, confirm the status bar (`packages/dsh-zeal/src/tui/ui/StatusBar.tsx`'s `⏵ running (esc interrupts)` / `idle` hints) returns to idle promptly, type a redirect message, confirm the next turn acts on the redirect rather than resuming the original task — cited against `App.tsx`'s Esc handler, `driver.ts`'s `interrupt()` (`liveAgent.cancel({kind:'user'},{keepInbox:true})`), and `dsh-agent`'s `CancelOptions`/`AgentStatus` doc. To record this row for real, follow `gauntlet/tasks/g8-interrupt/manual.md`'s numbered procedure with a real `ZAI_API_KEY` and an interactive terminal, and check off its pass/fail checklist. |
| smoke (`packages/dsh-zeal/tests/composition/live-smoke.test.ts`) | — | zai / glm-5.2 | 0.1.0 | 0.1.0-rc.6 | **PENDING (requires ZAI_API_KEY)** | Not yet run: no `ZAI_API_KEY` is available in this environment. The live-call test is double-gated (`ZAI_API_KEY` AND `ZEAL_COMPOSITION`) and confirmed to skip cleanly without a key — verified for real in this environment, along with a separate fake-credential probe (`ZEAL_COMPOSITION` only, no real key) that installs the same composed profile+overlay and confirms the run fails at the provider's own auth check (`dsh: AUTH: 401: {"code":"401","message":"token expired or incorrect"}`), proving the full chain up to the API boundary. To record this row for real — with `ZAI_API_KEY` exported in your environment, never inline on the command (shell history and CI command logs capture command text) — run:<br>`ZEAL_COMPOSITION=1 pnpm vitest run packages/dsh-zeal/tests/composition/live-smoke.test.ts` |
